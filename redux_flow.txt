1. 先将多个redux 整合成一个
2. 利用react-redux中的provider 和 connect 实现在router中传递参数
    基本思想是： a. 先在用户完成确认时传输该用户本次实验的信息
               b. 然后获得数据后向全局的store注册
3. 修改原有代码，在每次跳转进新的task前，将store中关于 ‘聊天记录’ 以及 ‘history， task’归位
